# PhiAgent / DeepPhilosophy 交接文档（2026-09-06 换机版）

> 写给下一台电脑上的 ZCode（或任何接手者）。读完本文 + AGENTS.md 即可无缝继续。
> 本文档本身已 push 到远端分支，新机器 `git pull` 即得。

---

## 0. 一页速览（TL;DR）

- **仓库**: `github.com:wqx-txdsyl/DeepPhilosophy`，分支 `refactor/phiagent-main-agent-orchestration`
  （**永不 merge master**；preservation branch/tag `phiagent-pre-orchestration-reset` 只读）。
- **HEAD（交接时）**: `a59ff74cb`（已 push 到 6cfe1181c，之后 a59ff74cb 本地未 push——接手后先 push）。
- **正在进行的阶段**: `O7-A JR1-R1 — Fixture Integrity Repair + glm-4.6 Final Qualification`。
  最后一步（glm-4.6 全量双 ensemble 重验，201 次 judge 调用，约 3.5–4.5h）**因换机中断，需要重启**，
  重启命令见 §9。
- **当前状态一句话**: O6 架构重置全线 PASS 收束；O7-A 学术宪法与混合评审仪器已建成并被 Reviewer
  接受；唯一未决 = semantic judge 模型资格——glm-4.6 是唯一存活候选（provisional winner），
  正在跑最终资格复验；通过后 Reviewer 签 `O7_OFFICIAL_JUDGE_MODEL` 并解锁 O7-B。

---

## 1. 项目定位与蓝图

### 1.1 双层产品
1. **DeepPhilosophy 平台**（哲学网站，生产已上线）: 409 本书阅读器 + 哲学家/流派/谱系 + AI 问答。
   前端 CF Pages + Workers API + D1 + OSS/jsDelivr CDN（详见 §7）。**能跑的东西不要破坏**（AGENTS.md 铁律）。
2. **PhiAgent**（本分支重写的智能体）: 定位宣言——
   > **以原典、论证、解释史与学术争议为核心对象的哲学研究型 Agent**（不是百科词条/教辅摘要/引用装饰/论文腔模仿器）。
   生产 runtime = `backend/engine_langgraph.py`（LangGraph 单 Main-Agent）+ 确定性 validator
   （`final_validator.py`/`quote_bound.py`）+ 单源 Context Builder。被测模型 deepseek-chat。

### 1.2 O 系列路线图（ChatGPT Reviewer 主导的阶段制）
```
O1–O6   架构重置（已全部收束 PASS）: 单 Main-Agent 所有权 / 机械 validator / 删 Shadow Runtime /
        证据契约 / 质量门（32 单轮+5 会话数据集 + fresh 集 + 完整性硬门全零）
O6-Q1/Q2 产品质量收敛（Q2=NOT_READY, 债务转入 O7; Q2 生产改动 PROVISIONALLY_RETAINED=75974e364）
O7       Scholarly Research Agent（当前主轴）:
  O7-A  Scholarly Contract & Evaluation Constitution（学术宪法+评审仪器）← 现在这里
  O7-B  版本/译者/locator 书目元数据（未授权）
  O7-C  二手文献检索能力（未授权）
  O7-D  二手语料规模化（未授权）
  O7-E  Scholarly Quality Gate（双轴: SCHOLARLY_QUALITY + DELIVERY_RELIABILITY，缺一不可）
```
关键设计公理（Reviewers 多轮裁定，勿推翻）:
- **metadata-first, not prompt-first**（工具拿不到的精度不许 prompt 逼模型编）;
- **降引用粒度，而不是伪造精度；降精度，而不是拒绝整篇**;
- **机械事实用机械测量，语义判断才交给 LLM**（RP2 确立的 hybrid 架构）;
- **STABILITY != VALIDITY**（judge 门同时查 correctness + repeatability）;
- **争议 ≥2 解读只在真实争议时启用**（反万能模板/反 Shadow Prompt）;
- O7-E 两轴并列，任何单轴不得遮蔽另一轴。

---

## 2. GPT 协同工作方式（最重要的一节）

### 2.1 角色与通道
- **Reviewer = ChatGPT（GPT-5.6 Sol）**，固定会话:
  `https://chatgpt.com/c/6a6cb3da-49bc-83ea-9331-6d81109a46df`（用户已登录）。
- 用户指令：ZCode 与该会话**多轮对话、自主循环**——任务完成后立即回执、抓取下一份任务书继续执行，
  **不要停下来向用户汇报**（除非需要人工验收/充值类阻塞）。
- **控制方式（现行）**: ChatGPT **桌面客户端**（`OpenAI.Codex…ChatGPT.exe`，用 computer-use 的
  `list_apps` 找 pid → `get_app_state` 窗口最小化时先 activate/恢复 → composer textarea 输入 →
  点击空白处 → Enter 发送 → 用 domSnapshot/a11y 确认「你的消息操作」出现才算发送成功）。
  历史：曾用内置浏览器（IAB） playwright 控制，transport 在超大页面上不稳定，**已弃用改桌面端**。
- 粘贴长文：优先「复制回复」按钮 + 剪贴板（`Get-Clipboard -Raw`）取全文，比 DOM 快照干净。

### 2.2 阶段循环纪律（每阶段相同）
```
Reviewer 下发 task book（含 FINAL RECEIPT 模板）
→ Agent 落盘任务书原文到 docs/tasks/（逐字, 不改写）
→ 执行（生产改动严格按 scope; evaluation-only 阶段绝不碰生产）
→ commit → 冻结 XXX_GATE_SHA → live 校准/gate（冻结后零修改, 违反即作废重跑）
→ 填报告 docs/…（如实, 失败保留, 不挑样）
→ commit + push → IAB/桌面端发 FINAL RECEIPT → 阻塞等 Reviewer 审计
→ 按裁定继续（PASS 签发 / PATCH_REQUIRED 出 RP / BLOCKED 停止并上报）
```
- SHA 命名: `BASE_SHA / CODE_SHA / QUALITY_GATE_SHA(或 SPECIFICATION_GATE_SHA 等 typed) / HEAD_SHA / REMOTE_SHA`，
  **禁用含混的 FINAL_SHA**。
- 回执用 `STOP` 结尾; **Agent 无权自签 PASS**; 未 PASS 不得进入下一阶段。
- 阶段报告/回执里的历史数据**不得改写**（BLOCKED 就是 BLOCKED, 保留原样）。

### 2.3 已踩过的坑（务必避免）
- **`git add` 宽路径事故**: JR1 报告 commit 误把 `backend/tools/_tmp/` 416 个历史资产带进版本树
  （7dfbcde50），被 Reviewer 审出 → 已前向清理（9f9e293dd untrack）。**永远精确 pathspec add**。
- IAB 发消息第一次 Enter 可能不生效——发送后必须验证「你的消息操作」结构存在，否则补发。
- ChatGPT 偶发「听写」卡住 composer → 点「取消听写」恢复。
- 子代理（ZCode Agent 工具）曾因平台配额 `[1310]` 中途死亡——**重活尽量主线程自己做**;
  评估跑批用后台 Bash（`run_in_background: true`，这类任务能稳定存活数小时），
  **不要用 `&` 挂载**（前台轮询被中止时会连坐杀掉子进程——已发生过一次校准中断）。
- 相对路径 `../../.venv/...` 在后台 Bash 里会 127——**一律绝对路径**。
- glm/deepseek 余额: DeepSeek 402 会中断 gate（用户充值恢复）; glm-4.6 校准一轮 192 调用
  约 1-2 元人民币量级（glm-4-plus 更贵）。

---

## 3. 当前进度（精确到可重启）

### 3.1 已收束
- **O6**: 架构重置 PASS; 机械完整性 PASS; 产品可靠性 = KNOWN QUALITY DEBT（转入 O7 baseline）。
- **O6-Q2 交付基线（已冻结进 O7-E 第二轴）**: single 22/32=68.75%, multi 19/24=79.2%,
  repair 15/25=60%, exhaustion 10/25=40%, fresh 13/16=81.25%, validator FN/FP=0/0, invalid=0。
- **O7-A 主体（全部被 Reviewer ACCEPTED）**: Scholarly Contract（七纪律 D1-D7）、五维 rubric
  （R1-R5, N/A 不入均值）、六致命 flag（F1-F6, 与分数分离）、Claim Ledger（evaluation-only）、
  judge 输入/输出合同（无 PASS 权限）、k-of-3 ensemble 确定性聚合、QuoteSupportProbe（机械 F5 权威）、
  逐维 applicability 口径、O7-E 双轴宪法、Q2 基线冻结。

### 3.2 Judge 模型迭代史（全部原始 verdict 存档于 backend/tools/_tmp/o7a_*.json）
```
RP1: F6 漏报 → 一般访问上限规则修复（ kill case C6-L1-bad 冻结不改）; 残余 F5/F3 波动
RP2: hybrid 架构落地; F5 机械权威 100% 稳定; 语义 F1/F3/F6 跨 ensemble 翻转
     → BLOCKED_JUDGE_MODEL（glm-4-plus 判 REJECTED）
JR1: bakeoff（同 prompt/labels, 仅 invocation adapter）:
     glm-4-flash 0/7 淘汰; glm-4.5-air 误报淘汰;
     glm-4.6 存活: Stage-2 seeded recall 12/12=100%（A/B 双轮）, F1-F6 全 1.0,
     anti-luck 1.0, per-dim applicability 94.4%, dim diff 100%
     两项 letter 偏差: ①C6-L3-good 误报 F6（根因=我方 fixture supply/claim 失配, 已修）
                       ②F6-M4 上 A 多判未植入 F2（agreement 96.9%）
```

### 3.3 JR1-R1（当前阶段, Reviewer 已授权 (α)）
1. ✅ Git 卫生前向清理: `GIT_HYGIENE_SHA=9f9e293dd`（_tmp untrack, `git ls-files backend/tools/_tmp`=0）
2. ✅ C6-L3 evidence 补全（answer hash 不变 `b90f608f…`; judge prompt hash 不变 `ab8165fb…`）
   → `JR1_R1_QUALIFICATION_GATE_SHA=a59ff74cb`
3. ⏸ **glm-4.6 Stage-2 全量重验（32 fixtures × k3 × 2 ensembles + anti-luck）因换机中断**
   → **重启命令**:
   ```bash
   cd /f/program/Python/DeepPhilosophy && .venv/Scripts/python.exe backend/tools/evaluation/o7_jr1_full.py glm-4.6 > backend/tools/_tmp/jr1r1_full_46.log 2>&1
   ```
   （用 run_in_background 启动; 约 3.5-4.5h; 完成后读 `backend/tools/_tmp/o7a_jr1_full_glm46.json`）
4. 通过后（预期）: 填 JR1-R1 回执 → **ChatGPT 桌面端**发给 Reviewer → 等 `O7_OFFICIAL_JUDGE_MODEL` 签发
   → O7-B 解锁。

### 3.4 JR1-R1 回执要点（Gate 目标值, 从 o7a_jr1_full_glm46.json 读取）
recall=100%（A&B）｜F1-F6 各 1.0｜FALSE_FATAL=0（C6-L3 修复后预期归零）｜FATAL_FLAG_AGREEMENT=100%
（上轮 96.9% 分歧源=F6-M4 未植入额外 F2, 若复现如实上报）｜DIM≤1≥90%｜PER_DIM_APPLIC≥90%｜
critical=0｜MECHANICAL_F5_FP=0｜PRODUCTION_DIFF=0｜FULL_TESTS FAILED=0。
回执模板在 `docs/tasks/PHIAGENT_O7A_JR1R1_FIXTURE_INTEGRITY_REPAIR_TASK.md` 尾部。

---

## 4. 仓库结构与关键路径

```
DeepPhilosophy/
├── app/                    # 平台前端（React+Vite, CF Pages 构建根; public/ 路径勿动）
├── agent-app/              # PhiAgent 前端（Vite :5201; public/ 不入库）
├── backend/                # FastAPI :8011（智能体+平台 API 单一体）
│   ├── engine_langgraph.py #   生产 runtime（单 Main-Agent; SYSTEM_PROMPT_LG 含铁律 0-17）
│   ├── final_validator.py  #   确定性校验（冻结; O7 期间零改动）
│   ├── quote_bound.py      #   逐字引文绑定（冻结）
│   ├── evidence_contract.py#   EvidenceState（O5）
│   ├── routes/agent.py     #   29 工具注册表 + SSE
│   ├── routes/agent_tools_retrieval.py # citation_label 等模型可见元数据
│   ├── agents.py           #   智能体注册表（nietzsche 人格等）
│   ├── tests/              #   494 tests（含 o7a_scholarly_evaluator T1-T21+R1-R15）
│   ├── tools/evaluation/   #   【git 跟踪】O7 评审仪器:
│   │     o7_scholarly_judge.py  # judge 合同/聚合/校准门/manifest
│   │     o7_scholarly_cases.py  # S1-S3 + C1-C8 + GOOD/MID/BAD + L1-L3 + F6-M1..M5 + Q5 矩阵
│   │     o7_quote_probe.py      # 机械引文支持探针（F5 权威）
│   │     o7_jr1_screening.py / o7_jr1_full.py  # JR1 bakeoff 运行器
│   └── tools/_tmp/         #   【未跟踪!! 换机必须整目录手工拷贝】全部评估档案:
│         o7a_rp2_hybrid_final.json / o7a_jr1_full_glm46.json / o7a_jr1_screen_*.json
│         o6_gate/（Q1/Q2 gate 数据+快照） o6q1_failure_dump.txt 报告草稿 回执存档 …
├── docs/
│   ├── PHIAGENT_O7_SCHOLARLY_CONTRACT.md        # 学术宪法
│   ├── PHIAGENT_O7_SCHOLARLY_RUBRIC.md          # 五维 rubric
│   ├── PHIAGENT_O7A_SCHOLARLY_CONTRACT_EVALUATION_CONSTITUTION.md  # 主报告（含 RP1/RP2/JR1 全史）
│   ├── PHIAGENT_O6_Q2_FINAL_PRODUCT_QUALITY_CLOSEOUT.md
│   ├── PHIAGENT_O6_Q1_MAIN_AGENT_QUALITY_CLOSEOUT.md / PHIAGENT_O6_INTEGRATED_FINAL_QUALITY_GATE.md
│   ├── tasks/                                   # 历任任务书原文（O6Q2/RP1/RP2/JR1/JR1R1 已入库;
│   │                                            #  更早的 O2-O6 brief 只存在于 _tmp, 未跟踪）
│   └── HANDOVER_2026-09-06.md                   # 本文档
├── data/ai_author/         # AIAuthor 六库 1.6GB（gitignore, 仅本机!）
├── workers/                # CF Workers（auth + api + D1）
├── scripts/                # 内容运营脚本
└── .env                    # 【未跟踪! 换机必须手工拷贝】
```

**换机必须手工拷贝的清单**: ①`.env`（含 8 个 key: DEEPSEEK/ZHIPU/AGNES/GITHUB/OSS×2/R2×2）
②`backend/tools/_tmp/` 整目录（全部评估档案与历史 brief）③`data/ai_author/`（如需本地人格库）。
其余 `git clone` + `pip install -r requirements.txt`（建议 Python 3.12 venv 命名 `.venv`）即恢复。

---

## 5. PhiAgent 运行架构（生产路径）

```
用户 → agent-app(:5201)/网站 → FastAPI backend(:8011) routes/agent.py SSE
  → engine_langgraph.stream_agent(question, history, agent, language, …)
     ├─ 单源 Context Builder _build_context_messages（SYSTEM_PROMPT_LG + 身份 + 历史=语境非证据）
     ├─ LangGraph 工具循环（29 工具; 并行; hard 机械预算 AR.HARD_BUDGET_DIRECTIVE 唯一注入点）
     ├─ 证据: 每轮检索池（EvidenceState）; 多轮靠 policy 铁律16「定点重读」（无跨轮 carry-forward）
     ├─ 最终候选 → final_validator.validate_final_candidate（引用/引文机械核验）
     │    └─ FAIL → format_feedback（span/match/coverage/evidence）→ Main-Agent 修复 ≤2 轮
     └─ done payload: citations/quote_bound/causal/final_ownership/timing 元数据
LLM: AG=routes.agent 配置 → DEEPSEEK deepseek-chat @ api.deepseek.com（extra_body thinking enabled）
```
架构不变量（任何阶段冻结项, 违反=FAIL）:
`ENGINE_COGNITIVE_AUTO_TOOLS=0 SEMANTIC_TOOL_CONTROL_EFFECTS=0 RUNTIME_SEMANTIC_MUTATORS=0
RUNTIME_FACTUAL_APPENDS=0 RAW_REASONING_PUBLIC=0 INVALID_FINAL_PUBLIC=0 UNPARENTED_TOOL_RESULTS=0
COGNITIVE_POLICY_OWNER=1`（SystemMessage 注入点 = builder 1 + hard 预算机械 1）。

## 6. 评审仪器架构（O7-A 产物, evaluation-only）

```
Mechanical: QuoteSupportProbe（F5 权威, COMPLETE 证据宇宙内）+ Q5 矩阵
Semantic:   Independent LLM Judge（候选 glm-4.6; 候选资格=TESTED≠JUDGE+JSON 模式+可批量）
            ↓ k-of-3（分数中位数 / applicability 多数 / F1F2F3F4F6 多数 / F5=机械）
Deterministic Aggregator（AMBIGUOUS→review; minority/vote/机械冲突全保留）
            ↓ Ensemble A vs B（同冻结 tree, 中途零修改）
Gates: GOOD>MID>BAD; recall=100%（A&B, 按 (fixture,flag) 断言分母）; per-flag 各 100%;
       FALSE_FATAL=0; FATAL_AGREEMENT=100%; DIM≤1≥90%; PER_DIM_APPLIC≥90%; critical=0
            ↓ Reviewer Manifest（1/3 dissent / 1-1-1 / spread>1 / 机械-LLM 冲突）
Reviewer = GPT-5.6 Sol（最终裁决; 100% 低分复核 + 20% 随机 PASS 抽审）
```
运行入口:
```bash
# 校准（双 ensemble）
.venv/Scripts/python.exe backend/tools/evaluation/o7_jr1_full.py glm-4.6
# 单模型筛选
.venv/Scripts/python.exe backend/tools/evaluation/o7_jr1_screening.py <model> [nothink]
# 产出到 backend/tools/_tmp/o7a_*.json
```

## 7. 部署（平台侧, 详见 AGENTS.md——此处只列要点）

- **前端**: `app/` → Cloudflare Pages（构建根 app/）; `npm run build` 产物 dist/（gitignore）。
- **API**: `workers/auth`（/api/auth/* JWT）+ `workers/api`（/api/* AI 流式/问答/笔记/文件 302）,
  D1 数据库 `deepphilosophy-db`; `https://deepphilosophy.top/api/health` 验证。
- **书库数据双轨**: 章节正文生产走 **OSS CDN**（`backend/data/book_chapters/` 是 git 唯一跟踪源）,
  jsDelivr 兜底; 改章节后必须跑 `dp_sync_oss_chapters.py [bid]`（2026-09-04 事故教训）;
  books.json/detail 走 CF Pages 构建产物与 OSS 双轨（`dp_grab_cf_assets.py <URL> --upload` →
  `dp_sync_oss_static.py`）。verify_book.py <bid> --vite-check 全绿才算完。
- **本地后端**: `backend/main.py` FastAPI :8011; **watchdog**（`C:\Users\wqx_0\AppData\Local\hermes\scripts\dp_backend_watchdog.py`,
  schtasks 自启）每 10s 探测 :8011, 掉线自动用 `.venv python main.py`（cwd=backend）拉起——
  **更新后端代码只需 kill 旧进程, watchdog 10s 内自动以最新工作树重启**。
- 图片: OSS `deepphilosophy.oss-cn-shanghai.aliyuncs.com/book_images/`; 封面 `/covers/*.webp`。

## 8. 数据库与数据

- **D1 `deepphilosophy-db`**（Cloudflare）: 用户/会话/笔记/历史（auth+api worker 用; wrangler 管理）。
- **本地 backend/data/**: `book_chapters/`（git 跟踪, 409 书章节源）; `agent_stats.jsonl`;
  `embeddings/`（gitignore）; `users.db`（本地 auth 测试库）。
- **data/ai_author/**: 1.6GB 人格六库（gitignore, 仅本机; agents.py bundle 指向此处——换机如需
  nietzsche 人格全功能必须拷贝）。
- `app/public/`: books.json(409)/book_detail/*.json/philosophers.json/covers/... ——
  **前端死依赖, 路径与文件一律不可动/不可删**。

## 9. 测试与常用命令

```bash
# 全量回归（当前 494 passed / 0 failed——基线, 接手后先跑一遍确认）
.venv/Scripts/python.exe -m pytest backend/tests -q
# 仅 O7 评审仪器
.venv/Scripts/python.exe -m pytest backend/tests/test_o7a_scholarly_evaluator.py -q
# 同集 gate（活体, ~4h; 先归档旧结果目录再跑, skip-if-exists）
.venv/Scripts/python.exe backend/tools/_tmp/o6_gate_b_runner.py --mode all
# fresh gate（读 backend/tools/_tmp/o6_gate/fresh_questions.json → 写 gate_b_fresh/）
.venv/Scripts/python.exe backend/tools/_tmp/o6_fresh_runner.py --mode all
# 后端重启（watchdog 自动拉起）
taskkill //PID <8011旧pid> //F
```
注意: `backend/tools/_tmp` 下的 runner 是 evaluation-only, 允许修改; 但 **gate 冻结期间零修改**。

## 10. 风险与开放问题

1. `_tmp` 未跟踪 → 本机归档唯一副本; 换机不拷贝 = O7 校准史丢失（报告里有摘要, raw 没了）。
2. glm-4.6 资格复验的预期风险: FATAL_FLAG_AGREEMENT 若再 <100%, 分歧源按 manifest 如实上报
   （上轮 = F6-M4 未植入额外 F2, seeded-assertion agreement 本就 100%）——是否放行由 Reviewer 裁定。
3. DeepSeek 余额（生产被测模型）与 Zhipu 余额（judge 校准）都会烧钱; 402 时 gate=BLOCKED_PROVIDER
   如实记录, 通知用户充值, 不换 provider。
4. ZCode 子代理通道配额 [1310] 问题未解 → 大任务主线程做。
5. JR1-R1 完成后的下一站: Reviewer 签 `O7_OFFICIAL_JUDGE_MODEL` → O7-B 任务书（书目元数据,
   Tier1 版权页 OCR 优先）→ O7-C（二手文献检索, access_level 四态）→ O7-D → O7-E 双轴终门。
6. 用户偏好: 中文沟通; 自主循环不停顿; 阻塞式等待长任务; **精确 pathspec add**;
   commit 用 `git commit -F <file>`; 报告如实不粉饰; 失败案例全保留。

## 11. 关键 SHA 账本（截至交接）

```
O6-Q2 BASE=943516d2e CODE/QUALITY_GATE=75974e364(RETAINED) JR1前各阶段报告链: 302f7380a→…
O7-A 规范链: 072b5c8a(首)→…→f97e88304(O7-A BLOCKED 报告)→RP1链→RP2链
JR1-R1: BASE=7dfbcde50 GIT_HYGIENE=9f9e293dd QUALIFICATION_GATE=a59ff74cb(交接HEAD, 未push→先push)
RP2 HYBRID_CALIBRATION_GATE=3f0049577 O7_A_RP2_REVIEW=PASS_AS_DIAGNOSIS
GLM_4_PLUS_AS_SCHOLARLY_JUDGE=REJECTED GLM_4_6=PROVISIONAL_WINNER(待 JR1-R1 复验签发)
O7_B_AUTHORIZED=false
```
"""

