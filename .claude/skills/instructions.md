## 全局执行规范

1. **工具调用优先**：在开始任何任务前，先调用 `TodoWrite` 创建任务清单。
2. **Skill 执行纪律**：读取 Skill 文件后，必须先调用 `TodoWrite` 注册其所有步骤，再执行步骤 1。
3. **禁止跳过**：禁止在未创建 Todo 清单的情况下直接执行操作。

> 这条指令对所有 Skill 和所有对话生效，优先级高于 Skill 内容。